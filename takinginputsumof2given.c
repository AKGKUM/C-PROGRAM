#include<stdio.h>
int main()
{
    int x, y, sum;
    printf(" enter first number : ");
    scanf("%d", &x);
    printf("ente second number : ");
    scanf("%d", &y);
    sum = x + y;
    printf("the sum of 2 given value is = %d", sum);
    return 0;
}